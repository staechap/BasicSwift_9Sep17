var//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"

var nameString = "Supachai"

// เป็นการกำหนด ตัวแปร แบบ statics final

var surnameString = "Taechapisit"

// ลอง ทำการ assign ค่าใหม่ ให้ กับ ตัวแปร surnameString
// ซึ่งจะ ทำไม่ได้ ต้องเปลี่ยน ให้เป็น var

surnameString = "AAA"

// print
print("Name ==> \(nameString)")

// About Integer

let number1Aint = 5

let number2Aint: Int = 10  // การประกาศแบบกำหนด type ของ data ว่าเป็น int

let answer = number1Aint+number2Aint

print("Answer ==>\(answer)")






